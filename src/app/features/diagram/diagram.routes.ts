import { Routes } from '@angular/router';
import { DiagramPageComponent } from './pages/diagram-page/diagram-page.component';

export const DIAGRAM_ROUTES: Routes = [
  { path: '', component: DiagramPageComponent },
];
